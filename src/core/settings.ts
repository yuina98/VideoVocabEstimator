/** 用户词汇量设置项的 storage key（chrome.storage.local） */
export const USER_VOCAB_KEY = 'userVocab';

/** 目标语速(WPM)设置项的 storage key；未设置表示不控制播放速度 */
export const WPM_KEY = 'targetWpm';

/** 是否只减速不加速（目标语速高于自然语速时不调整） */
export const SLOW_ONLY_KEY = 'wpmSlowOnly';
